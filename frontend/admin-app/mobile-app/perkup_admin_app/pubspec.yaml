name: perkup_admin_app
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
   
  
  http: any
  # bloc: any
  # flutter_bloc: any
  # equatable: any
  provider : any


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # generate: true

  

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
